### 1. 视觉设计系统

### 1.1 颜色规范

```
主色调:
- Primary: #007AFF (录制按钮、重要操作)
- Secondary: #5856D6 (AI功能相关)
- Success: #34C759 (成功状态、权限已授予)
- Warning: #FF9500 (警告、等待状态)
- Error: #FF3B30 (错误、权限拒绝)
- Background: #F2F2F7 (浅色模式背景)
- Card: #FFFFFF (卡片背景)
- Text Primary: #000000
- Text Secondary: #8E8E93

```

### 1.2 字体规范

```
标题层级:
- H1: SF Pro Display, 28px, Bold (页面主标题)
- H2: SF Pro Display, 22px, Semibold (区块标题)
- H3: SF Pro Display, 18px, Medium (卡片标题)
- Body: SF Pro Text, 14px, Regular (正文)
- Caption: SF Pro Text, 12px, Regular (辅助信息)

```

### 1.3 间距规范

```
Spacing Scale:
- xs: 4px
- sm: 8px
- md: 16px
- lg: 24px
- xl: 32px
- xxl: 48px

组件间距:
- 页面边距: 24px
- 卡片内边距: 16px
- 按钮内边距: 12px 24px
- 表单项间距: 16px

```

### 2. 组件设计规范

### 2.1 按钮规范

```
Primary Button:
- 背景: #007AFF
- 文字: #FFFFFF
- 圆角: 8px
- 高度: 44px
- 悬停: #0056CC

Secondary Button:
- 背景: transparent
- 边框: 1px solid #007AFF
- 文字: #007AFF
- 圆角: 8px
- 高度: 44px

Danger Button:
- 背景: #FF3B30
- 文字: #FFFFFF
- 圆角: 8px

```

### 2.2 卡片组件

```
Card:
- 背景: #FFFFFF
- 圆角: 12px
- 阴影: 0 2px 8px rgba(0,0,0,0.1)
- 边框: none
- 内边距: 16px

```

### 2.3 状态指示器

```
Status Indicator:
- 成功: 🟢 绿色圆点 + "已连接"
- 警告: 🟡 黄色圆点 + "等待中"
- 错误: 🔴 红色圆点 + "未连接"
- 进行中: 🔵 蓝色圆点 + 动画

```

### 3. 具体组件尺寸

### 3.1 侧边栏导航

```
Sidebar:
- 宽度: 200px (固定)
- 背景: #F8F9FA
- 导航项高度: 44px
- 图标大小: 20x20px
- 选中状态: 背景 #E3F2FD

```

### 3.2 音频波形显示

```
Audio Waveform:
- 高度: 60px
- 条形宽度: 2px
- 条形间距: 1px
- 活跃颜色: #007AFF
- 非活跃颜色: #E5E5EA

```

### 3.3 转录文本区域

```
Transcript Area:
- 行高: 1.5
- 时间戳宽度: 80px
- 时间戳颜色: #8E8E93
- 选中背景: #007AFF20
- 滚动条样式: macOS 原生

```

### 4. 交互状态设计

### 4.1 悬停状态

- 按钮: 背景色加深 10%
- 卡片: 阴影加深
- 列表项: 背景变为 #F5F5F5

### 4.2 激活状态

- 按钮: 背景色加深 20%
- 输入框: 边框变为主色调

### 4.3 禁用状态

- 透明度: 50%
- 鼠标样式: not-allowed

### 5. 图标规范

### 5.1 图标库

```
系统图标 (SF Symbols):
- 录制: record.circle
- 暂停: pause.circle
- 停止: stop.circle
- 播放: play.circle
- 设置: gear
- 历史: clock
- 麦克风: mic
- 音频: speaker.wave.2
- AI: brain.head.profile
- 导出: square.and.arrow.up

```

### 5.2 图标尺寸

- 导航图标: 20x20px
- 操作按钮图标: 16x16px
- 状态图标: 12x12px

### 6. 动画规范

### 6.1 过渡动画

```
Transitions:
- 页面切换: 200ms ease-out
- 卡片悬停: 150ms ease-out
- 按钮交互: 100ms ease-out
- 模态窗口: 300ms ease-out

```

### 6.2 录制状态动画

- 录制按钮: 呼吸效果 (1s 循环)
- 音频波形: 实时更新 (60fps)
- 状态指示器: 脉冲效果

### 7. 响应式断点

```
Window Sizes:
- 最小宽度: 800px
- 舒适宽度: 1000px
- 最大宽度: 无限制

Layout Breakpoints:
- < 900px: 侧边栏收起为图标
- < 800px: 显示最小化布局

```

## 补充后的设计交付清单

✅ **有了这些规范，设计师可以设计出：**

1. 完整的视觉设计系统
2. 所有页面的高保真设计稿
3. 组件库和设计规范文档
4. 交互原型
5. 开发标注文档

✅ **设计师还需要考虑：**

1. Dark Mode 适配
2. 无障碍访问 (Accessibility)
3. 多语言界面适配
4. 错误状态页面设计
5. 加载状态设计

这样设计师就有足够的信息来创建完整的设计方案了！

### 8. 无障碍设计规范

### 8.1 颜色对比度
```
WCAG 2.1 标准:
- 普通文本: 4.5:1
- 大号文本: 3:1
- 用户界面组件: 3:1
- 装饰性元素: 无要求
```

### 8.2 键盘导航
```
- Tab 键顺序: 符合视觉阅读顺序
- 焦点状态: 2px 蓝色边框 (#007AFF)
- 快捷键: 提供常用操作的快捷键
- 跳过导航: 提供跳过重复导航的选项
```

### 8.3 屏幕阅读器支持
```
- 所有图片提供 alt 文本
- 表单控件提供 aria-label
- 动态内容提供 aria-live 区域
- 使用语义化 HTML 标签
```

### 9. 深色模式规范

### 9.1 颜色映射
```
主色调:
- Primary: #0A84FF (深色模式)
- Secondary: #5E5CE6 (深色模式)
- Success: #30D158 (深色模式)
- Warning: #FF9F0A (深色模式)
- Error: #FF453A (深色模式)
- Background: #1C1C1E (深色模式)
- Card: #2C2C2E (深色模式)
- Text Primary: #FFFFFF
- Text Secondary: #8E8E93
```

### 9.2 深色模式适配
```
- 阴影: 使用更柔和的阴影效果
- 透明度: 调整透明度以适应深色背景
- 图片: 提供深色模式下的图片版本
- 图标: 调整图标颜色以适应深色背景
```

### 10. 响应式设计扩展

### 10.1 设备断点
```
- 移动端: < 768px
- 平板: 768px - 1024px
- 桌面: > 1024px
- 大屏: > 1440px
```

### 10.2 响应式字体
```
移动端:
- H1: 24px
- H2: 20px
- H3: 16px
- Body: 14px

平板:
- H1: 26px
- H2: 22px
- H3: 18px
- Body: 15px

桌面:
- H1: 28px
- H2: 24px
- H3: 20px
- Body: 16px
```

### 11. 错误状态设计

### 11.1 错误提示
```
- 内联错误: 红色文本 + 图标
- 表单错误: 红色边框 + 错误提示
- 全局错误: 顶部通知条
- 网络错误: 全屏错误页面
```

### 11.2 加载状态
```
- 页面加载: 骨架屏
- 按钮加载: 加载动画
- 列表加载: 无限滚动
- 操作加载: 进度指示器
```

### 12. 动画规范扩展

### 12.1 动画曲线
```
- 标准: cubic-bezier(0.4, 0, 0.2, 1)
- 强调: cubic-bezier(0.4, 0, 0.6, 1)
- 减速: cubic-bezier(0, 0, 0.2, 1)
- 加速: cubic-bezier(0.4, 0, 1, 1)
```

### 12.2 手势动画
```
- 滑动: 300ms
- 缩放: 250ms
- 旋转: 400ms
- 拖拽: 实时跟随
```

### 13. 国际化设计

### 13.1 文本处理
```
- 预留 30% 额外空间
- 支持文本换行
- 提供文本截断方案
- 考虑不同语言字体
```

### 13.2 RTL 支持
```
- 镜像布局
- 调整图标方向
- 适配文本对齐
- 调整动画方向
```

### 14. 性能优化

### 14.1 资源优化
```
- 图片: WebP 格式
- 图标: SVG 格式
- 字体: 子集化
- 动画: 使用 transform
```

### 14.2 加载策略
```
- 懒加载
- 预加载关键资源
- 按需加载组件
- 资源缓存策略
```

### 15. 设计系统维护

### 15.1 版本控制
```
- 语义化版本号
- 更新日志
- 变更记录
- 废弃标记
```

### 15.2 文档规范
```
- 组件使用说明
- 设计原则
- 最佳实践
- 常见问题
```

### 16. 开发协作

### 16.1 交付规范
```
- 设计稿标注
- 组件说明
- 交互说明
- 资源文件
```

### 16.2 评审流程
```
- 设计评审
- 开发评审
- 测试评审
- 发布评审
```

### 16.3 变更管理
```
- 变更申请
- 影响评估
- 实施计划
- 回滚方案
```

这样设计师和开发团队就有更完整的设计规范指导了！